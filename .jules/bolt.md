## 2026-04-12 - Component Re-rendering Optimization\n**Learning:** In Next.js components, passing static variables (like arrays or objects) inside the component body triggers reallocation on every render, causing expensive filtering functions like `Array.filter` to execute repeatedly. \n**Action:** Use `useMemo` to wrap expensive filtering logic based on state, and move static declarations outside the component to keep the reference stable.
